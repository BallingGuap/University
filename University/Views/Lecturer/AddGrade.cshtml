@model  University.Models.Student
@{
    ViewBag.Title = "AddGrade";
}

<h2>Add new grade to @Model.Account.FirstName</h2>

<div>
    @using (Html.BeginForm())
    {
        
        @Html.Hidden("studentId", Model.StudentId);

    <div class="form-group">
        @*@Html.Label("Subject", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.Editor("Subject", new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessage("Subject", "", new { @class = "text-danger" })
        </div>*@

        @Html.Label("Subject", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            <select class="form-control" id="Subject" name="Subject">
                @foreach (var s in Model.Subjects)
                {
                    <option>@s.SubjectName</option>
                }
            </select>
         </div>
        </div>

        <div class="form-group">
            @Html.Label("GradeNumber", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.Editor("GradeNumber", new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessage("GradeNumber", "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
        }

    </div>
